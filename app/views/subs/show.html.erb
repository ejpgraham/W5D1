<h1><%= @sub.title %></h1>
<p><%= @sub.description %></p>

<!-- TODO: POSTS HERE -->
<ul>

<% @sub.posts.each do |post| %>

  <li> <a href="<%= post_url(post) %>"><%= post.title %></a></li>

<% end %>
</ul>
<a href="<%= new_sub_post_url(@sub) %> ">Add post</a>

<a href="<%= edit_sub_url(@sub) %>">Edit sub</a>
<a href="<%= subs_url %>">All subs</a>
